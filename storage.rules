service firebase.storage {
  match /b/{bucket}/o {
    match /image/portfolio {
      allow read;
      allow write: if isLoginUser()
    }
    match /image/etc {
      allow read;
    }

    function isLoginUser() {
    	return request.auth != null
    }
  }
}
